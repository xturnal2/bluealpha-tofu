# Templates

Published stacks will live at `templates/<cloud>/<stack>/`.

Each stack is added in a focused change containing its OpenTofu configuration,
example inputs, stack-specific documentation, and validation coverage. Planned
stacks are tracked in the repository [roadmap](../ROADMAP.md).
